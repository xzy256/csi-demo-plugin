# csi-demo-plugin

```
1. base on one image build docker image
docker build -t xxxxxxx:5000/online/test/csi-demo-driver:v1.0 .
2. cd deploy/kubernetes
kubectl apply -f xxx.yaml
3. edit deploy/example xxx.yaml, kubectl apply it
```
